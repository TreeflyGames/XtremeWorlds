build-windows:
    name: Build for Windows
    needs: [generate-keys]
    runs-on: windows-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        submodules: recursive